# @format

测试 webpack 玩儿的

第一步: 一个入口，打包成一个大的 bundle 文件；
  - webpack compiler 的基本执行逻辑结构:
    1: 初始化各种参数，创建 compiler 对象，初始化编译环境（包括注入内置插件、注册各种模块工厂、初始化 RuleSet 集合、加载配置的插件等。）
    2: 构建 => 得到模块编译产物和依赖关系图
    3: 生成阶段
第二步: 一个大 bundle 玩个鸡儿。要把大 bundle 分开；